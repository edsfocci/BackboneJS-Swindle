source 'https://rubygems.org'
ruby '1.9.3'

gem 'rails', '3.2.14'

# Database
gem 'pg'

# A better IRB for Rails
gem 'pry-rails'

# Authentication
gem 'figaro'
gem 'devise'


# Gems used only for assets and not required
# in production environments by default.
group :assets do
  # Superset of CSS
  gem 'sass-rails',   '~> 3.2.3'
  # Superset of JavaScript, probably not useful to learn yet
  # gem 'coffee-rails', '~> 3.2.1'

  # JavaScript parser/compressor/beautifier
  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# Use unicorn as the app server; optimization I'll need in the distant future
# gem 'unicorn'

# To use debugger
# gem 'debugger'
